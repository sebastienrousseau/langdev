#!/usr/bin/env bash
# SPDX-License-Identifier: Apache-2.0 OR MIT
# Test double for make — records argv and exits 0.
{ printf '%s' "$0"; printf ' %s' "$@"; printf '\n'; } >> "${STUB_LOG:-/dev/null}"
exit 0
